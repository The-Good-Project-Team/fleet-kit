{
  "name": "roomba",
  "emoji": "🧹",
  "kind": "mechanical",
  "lane": "hygiene",
  "mandate": {
    "target": "every stale/orphaned worktree and every ghost (dead/stale/unparseable) crew job gets removed or flagged, with zero false-positive removals of in-flight work",
    "checklist": [
      "list every worktree except the primary one",
      "a worktree is a removal candidate ONLY if ALL hold: branch merged (ancestor OR patch-equivalent squash-merge) into the default branch, OR provably abandoned (pushed+synced, no open PR, commit older than the stale-days threshold)",
      "tree must be clean (no uncommitted/untracked changes) before removal",
      "branch must not be in the protect list",
      "worktree must be at least the min-age threshold old, so an in-flight pass is never swept mid-task",
      "separately: a dangling builder worktree (path convention embeds its own spawning PID) is a candidate regardless of merge/dirty state once that PID is confirmed dead",
      "an orphaned registry entry (directory already gone from disk) only removes its branch once merge-base confirms the default branch already has everything it had",
      "for crew health: read the roster, mark a job NOT_LOADED/STALE/CRASHLOOP/UNPARSEABLE as a high-severity ghost",
      "dedup ghost alerts by kind+label so a persisting ghost doesn't refile every run -- only a NEW ghost or a kind transition alerts",
      "dry-run by default -- only remove/act when explicitly run with an execute flag"
    ],
    "limits": {"timeout_s": 600, "max_budget_usd": 0},
    "escalation": "a worktree or branch is ambiguous under the safety checks (age unknown, merge-base undeterminable because gh/network is unreachable) -- always KEEP, never guess toward removal. File a backlog item for a ghost that persists past N consecutive runs instead of alerting forever on the same one."
  },
  "schedule": {
    "interval_s": 3600
  },
  "timeout_s": 600,
  "enabled": false,
  "run_file": "roomba.py",
  "report": {
    "vision_link": "optional"
  }
}
